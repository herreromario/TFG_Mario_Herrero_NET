<Window x:Class="StockMeal.Frontend.Dialogos.EditarProductoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar producto"
        Height="420"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#232323">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TituloText" Grid.Row="0" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,16"/>
        <TextBlock Grid.Row="1" Text="Nombre" Foreground="White"/>
        <TextBox Grid.Row="2" Margin="0,4,0,10" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="3" Text="Descripción" Foreground="White"/>
        <TextBox Grid.Row="4" Margin="0,4,0,10" Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"/>

        <Grid Grid.Row="5" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                <TextBlock Text="Precio" Foreground="White"/>
                <TextBox Margin="0,4,0,0" Text="{Binding Precio, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock x:Name="CampoExtraLabel" Foreground="White"/>
                <TextBox x:Name="CampoExtraText" Margin="0,4,0,0"/>
                <ComboBox x:Name="UnidadCombo" Margin="0,4,0,0" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar" Width="120" Height="36" Margin="0,0,10,0" Click="Cancelar_Click"/>
            <Button Content="Actualizar" Width="120" Height="36" Click="Actualizar_Click"/>
        </StackPanel>
    </Grid>
</Window>
