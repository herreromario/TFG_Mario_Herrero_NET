<UserControl x:Class="StockMeal.Frontend.UserControls.UCListarClientes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Loaded="UCListarClientes_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lista de Clientes" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,20"/>
        <StackPanel Orientation="Horizontal" Margin="20" Grid.Row="1">
            <TextBlock Text="Buscar por nombre:" FontSize="16" VerticalAlignment="Center" Foreground="White" Margin="0,0,10,0"/>
            <TextBox Width="300" Height="30" FontSize="16" Text="{Binding textoNombreCliente, UpdateSourceTrigger=PropertyChanged}" TextChanged="textoNombreCliente_TextChanged"/>
        </StackPanel>
        <Button Content="Añadir cliente" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,20,0" Click="btnAñadirCliente_Click"/>
        <DataGrid x:Name="dgClientes" Grid.Row="2" ItemsSource="{Binding listaClientes}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ColumnWidth="*" PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Editar" Click="EditarCliente_Click"/>
                    <MenuItem Header="Eliminar" Click="EliminarCliente_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding Apellido}"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoCliente}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
