<UserControl x:Class="StockMeal.Frontend.UserControls.UCListarIngredientes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Loaded="UCListarIngredientes_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lista de Ingredientes" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,20"/>

        <StackPanel Orientation="Horizontal" Margin="20" Grid.Row="1">
            <TextBlock Text="Buscar por nombre:" FontSize="16" VerticalAlignment="Center" Foreground="White" Margin="0,0,10,0"/>
            <TextBox Width="300" Height="30" FontSize="16" Text="{Binding textoNombreIngrediente, UpdateSourceTrigger=PropertyChanged}" x:Name="textoNombreIngrediente" TextChanged="textoNombreIngrediente_TextChanged"/>
        </StackPanel>

        <Button Content="Añadir ingrediente" Grid.Row="1" HorizontalAlignment="Right" x:Name="btnAñadirIngrediente" Click="btnAñadirIngrediente_Click" Margin="0,0,20,0" Style="{StaticResource PrimaryButtonStyle}"/>

        <DataGrid x:Name="dgIngredientes" Grid.Row="2" ItemsSource="{Binding listaIngredientes}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ColumnWidth="*" PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Editar" Click="EditarIngrediente_Click"/>
                    <MenuItem Header="Eliminar" Click="EliminarIngrediente_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Precio}"/>
                <DataGridTextColumn Header="Stock disponible" Binding="{Binding StockDisponible}"/>
                <DataGridTextColumn Header="Stock mínimo" Binding="{Binding StockMinimo}"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
