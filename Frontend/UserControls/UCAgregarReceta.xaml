<UserControl x:Class="StockMeal.Frontend.UserControls.UCAgregarReceta"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Loaded="UCAgregarReceta_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nueva Receta"
                   FontSize="36"
                   FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,24"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <ComboBox Width="320" Margin="0,0,16,0" FontSize="16"
                      ItemsSource="{Binding listaPlatos}"
                      DisplayMemberPath="Nombre"
                      SelectedValuePath="IdProducto"
                      SelectedValue="{Binding idPlatoSeleccionado, UpdateSourceTrigger=PropertyChanged}"
                      materialDesign:HintAssist.Hint="Plato"/>
            <ComboBox Width="320" Margin="0,0,16,0" FontSize="16"
                      ItemsSource="{Binding listaIngredientes}"
                      DisplayMemberPath="Nombre"
                      SelectedValuePath="IdProducto"
                      SelectedValue="{Binding idIngredienteSeleccionado, UpdateSourceTrigger=PropertyChanged}"
                      materialDesign:HintAssist.Hint="Ingrediente"/>
            <TextBox Width="140" Margin="0,0,16,0" FontSize="16"
                     Text="{Binding cantidadIngrediente, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Cantidad"/>
            <Button Content="Añadir línea"
                    Width="150"
                    Click="btnAnadirLinea_Click"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Quitar línea"
                    Width="150"
                    Margin="12,0,0,0"
                    Click="btnQuitarLinea_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding nuevasLineas}"
                  SelectedItem="{Binding lineaSeleccionada, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ColumnWidth="*"
                  Margin="0,0,0,16">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Plato" Binding="{Binding IdProducto}"/>
                <DataGridTextColumn Header="Ingrediente" Binding="{Binding IdIngrediente}"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Guardar receta"
                    Width="180"
                    Height="48"
                    Margin="0,0,20,0"
                    Click="btnGuardarReceta_Click"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Cancelar"
                    Width="180"
                    Height="48"
                    Click="btnCancelar_Click"
                    Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
