<UserControl x:Class="StockMeal.Frontend.UserControls.UCEditarRol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="1560" d:DesignHeight="912">
    <Grid VerticalAlignment="Center"><StackPanel Width="520">
        <TextBlock Text="Editar Rol" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,40"/>
        <TextBox Margin="0,0,0,12" Text="{Binding rol.Nombre, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Margin="0,0,0,12" Text="{Binding rol.Descripcion, UpdateSourceTrigger=PropertyChanged}"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><Button Content="Actualizar" Width="180" Height="46" Margin="0,0,12,0" Click="Guardar_Click"/><Button Content="Cancelar" Width="180" Height="46" Click="Cancelar_Click"/></StackPanel>
    </StackPanel></Grid>
</UserControl>
