<UserControl x:Class="StockMeal.Frontend.UserControls.UCListarMovimientosCaja"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Loaded="UCListarMovimientosCaja_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Movimientos de Caja" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,20"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding listaMovimientos}" AutoGenerateColumns="False" CanUserAddRows="False" ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoMovimiento}"/>
                <DataGridTextColumn Header="Importe" Binding="{Binding Importe}"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}"/>
                <DataGridTextColumn Header="Compra" Binding="{Binding IdCompra}"/>
                <DataGridTextColumn Header="Venta" Binding="{Binding IdTransaccion}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
