<UserControl x:Class="StockMeal.Frontend.UserControls.UCDetalleVenta"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Titulo}" FontSize="34" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,20"/>
        <TextBlock Grid.Row="1" Text="{Binding Resumen}" FontSize="16" Foreground="White" Margin="0,0,0,14"/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Lineas}" AutoGenerateColumns="False" CanUserAddRows="False" ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Producto" Binding="{Binding Producto}"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Volver al historial" Width="210" Height="44" HorizontalAlignment="Right" Margin="0,16,0,0" Click="btnVolver_Click"/>
    </Grid>
</UserControl>
