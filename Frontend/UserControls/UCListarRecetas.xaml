<UserControl x:Class="StockMeal.Frontend.UserControls.UCListarRecetas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Loaded="UCListarRecetas_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Lista de Recetas"
                   FontSize="36"
                   FontWeight="Bold"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,20"/>

        <Button Content="Añadir receta"
            Grid.Row="1"
            HorizontalAlignment="Right"
            Margin="0,0,20,10"
            Click="btnAgregarReceta_Click"
            Style="{StaticResource PrimaryButtonStyle}"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding listaResumenRecetas}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id Plato" Binding="{Binding IdProducto}"/>
                <DataGridTextColumn Header="Plato" Binding="{Binding Plato}"/>
                <DataGridTextColumn Header="Nº ingredientes" Binding="{Binding Ingredientes}"/>
                <DataGridTextColumn Header="Cantidad total receta" Binding="{Binding TotalCantidad}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
