<UserControl x:Class="StockMeal.Frontend.UserControls.UCListarEmpleados"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Loaded="UCListarEmpleados_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Text="Empleados" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,20"/>
        <Button Content="Añadir empleado" HorizontalAlignment="Right" Margin="0,20,20,0" Click="Anadir_Click"/>
        <DataGrid x:Name="dg" Grid.Row="1" ItemsSource="{Binding listaEmpleados}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ColumnWidth="*" PreviewMouseRightButtonDown="DataGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Editar" Click="Editar_Click"/>
                    <MenuItem Header="Eliminar" Click="Eliminar_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding Apellido}"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}"/>
                <DataGridTextColumn Header="Rol" Binding="{Binding IdRolNavigation.Nombre}"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
