<UserControl x:Class="StockMeal.Frontend.UserControls.UCAñadirProveedor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Loaded="AñadirProveedor_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Añadir Proveedor" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,40"/>
        <TextBox Grid.Row="1" Width="420" FontSize="20" Text="{Binding proveedor.Nombre, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Nombre" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="White"/>
        <TextBox Grid.Row="2" Width="420" FontSize="20" Margin="0,16,0,0" Text="{Binding proveedor.Telefono, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Teléfono" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="White"/>
        <TextBox Grid.Row="3" Width="420" FontSize="20" Margin="0,16,0,0" Text="{Binding proveedor.Direccion, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Dirección" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="White"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
            <Button Content="Guardar" Width="180" Height="48" Margin="0,0,20,0" Click="btnGuardar_Click" Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Cancelar" Width="180" Height="48" Click="btnCancelar_Click" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
