<UserControl x:Class="StockMeal.Frontend.UserControls.UCListarCompras"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Loaded="UCListarCompras_Loaded"
             mc:Ignorable="d"
             d:DesignWidth="1560"
             d:DesignHeight="912">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Historial de Compras" FontSize="36" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,20"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding listaCompras}" AutoGenerateColumns="False" CanUserAddRows="False" ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdCompra}"/>
                <DataGridTextColumn Header="Fecha compra" Binding="{Binding FechaCompra, StringFormat=dd/MM/yyyy HH:mm}"/>
                <DataGridTextColumn Header="Proveedor" Binding="{Binding Proveedor}"/>
                <DataGridTextColumn Header="Producto" Binding="{Binding Producto}"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}"/>
                <DataGridTextColumn Header="Precio unitario" Binding="{Binding PrecioUnitario}"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}"/>
                <DataGridTemplateColumn Header="Detalle">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Ver detalle" Click="btnVerDetalle_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
